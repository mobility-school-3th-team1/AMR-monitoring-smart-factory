# 엔터티 및 관계 정의

- 주요 Entity
    - AREA(구역): 공장 운영을 위한 단위 구역 정의
        - e.g.) 창고, 건조 구역, 조립 구역, 검사 구역 등
        - 최대 허용 온도, 최대 허용 습도 등에 대한 정보가 포함됨
        - ENV_SENSOR(센서 정의)와 ENV_SENSOR_LOG(센서값 기록)를 통해 구역별 환경 정보 모니터링
    - PRODUCT(제품): 제품 정의
    - PROCESS_MASTER(공정): 공정 이름, 수행 구역 정의
        - ROUTING을 통해 제품별 공정 순서 정의
    - WORK_ORDER(주문)
        - 주문에 따라 생산 단위 WIP_LOT가 결정됨
    - AMR_MASTER: AMR 이름, 누적 주행 거리, 최종 점검일 등 저장
        - AMR_STATUS_LOG: 시뮬레이션·관제 기준 **운행 상태·위치 스냅샷** (물리 DDL: `DB/init.sql`)
        - AMR_COMMAND: FE→BE 제어 명령 이력 (`accepted`는 DB 반영 완료)
        - AMR_TASK: AMR별 할당 작업(비상 정지 시 정책에 따라 상태 정리)
    - AMR_CHARGE_STATION: 충전 스테이션 위치, 상태 등 저장
        - AMR_CHARGING_LOG를 통해 충전 기록 관리
- 주요 모니터링 요소
    - 공장 환경: 공장 내 온도, 습도, 먼지 농도
    - AMR 위치 및 상태: AMR 운영 상태, IMU(x, y, yaw), 적재 중량, 배터리 잔량, 온도, 건강도, 누적 주행거리
    - 제조 데이터: AMR 작업 목록, 주문/생산량

# ERD 작성

```mermaid
---
config:
  theme: base
  layout: elk
---
erDiagram
	direction TB
	SITE {
		varchar site_id PK "공장 ID"
		varchar site_name "공장명"
	}

	AREA {
		varchar area_id PK "구역 ID"
		varchar site_id FK "공장 ID"
		varchar area_name "구역명"
		varchar area_type "구역 타입"
		decimal temp_min "최소 허용 온도"
		decimal temp_max "최대 허용 온도"
		int humidity_min "최소 허용 습도"
		int humidity_max "최대 허용 습도"
		int particle_min "최소 허용 먼지"
		int particle_max "최대 허용 먼지"
		int co_gas_min "최소 허용 CO"
		int co_gas_max "최대 허용 CO"
	}

	ENV_SENSOR {
		varchar env_sensor_id PK "센서 ID"
		varchar area_id FK "구역 ID"
		string sensor_name "센서명"
		string sensor_type "센서유형"
		varchar unit "단위"
		decimal min_val "하한값"
		decimal max_val "상한값"
		date install_at "설치일"
	}

	ENV_SENSOR_LOG {
		bigint sensor_log_id PK "계측 ID"
		varchar env_sensor_id FK "센서 ID"
		decimal sensor_value "센서값"
		string sensor_status "센서 상태"
		datetime measured_at "계측 시각"
	}

	PRODUCT {
		varchar product_id PK "제품 ID"
		string product_name "제품명"
	}

	PR_PROCESS {
		varchar pr_process_id PK "공정 ID"
		string process_name "공정명"
		varchar area_id FK "구역 ID"
	}

	PR_ROUTING {
		varchar pr_routing_id PK "공정 레시피 ID"
		varchar product_id FK "제품 ID"
		varchar pr_process_id FK "공정 ID"
		int seq_no "공정 순서"
		int standard_lead_time "표준 소요시간(분)"
	}

	WORK_ORDER {
		bigint work_id PK "작업 ID"
		varchar product_id FK "제품 ID"
		int planned_qty "수량"
		date planned_start_date "계획 시작일"
		date planned_end_date "계획 종료일"
		varchar status "상태"
	}

	WIP_LOT {
		bigint lot_id PK "LOT ID"
		bigint work_id FK "작업 ID"
		int current_qty "수량"
		varchar pr_routing_id FK "현재 라우팅 단계(ROUTING)"
		varchar status "대기/작업중/이동중"
	}

	AMR_MASTER {
		int amr_id PK "AMR ID"
		string amr_name "AMR 이름"
		double total_mileage "총 주행 거리"
		int load_max "최대 적재 중량"
		int battery_capacity "배터리 용량"
		date inspection_dt "최종 점검일"
	}

	AMR_CHARGE_STATION {
		int station_id PK "스테이션 ID"
		varchar area_id FK "구역 ID"
		string station_name "스테이션 이름"
		varchar station_status "스테이션 상태(대기/충전)"
	}

	AMR_CHARGING_LOG {
		bigint charging_session_id PK "충전 기록 ID"
		int amr_id FK "AMR ID"
		int station_id FK "스테이션 ID"
		varchar session_status "충전 상태"
		datetime start_time "충전 시작 시각"
		datetime end_time "충전 종료 시각"
	}

	AMR_TASK {
		bigint task_id PK "작업 ID"
		int amr_id FK "AMR ID"
		varchar task_type "작업 유형(운송/충전)"
		bigint lot_id FK "Lot ID(WIP_LOT)"
		varchar from_area_id FK "출발 구역 ID(AREA)"
		varchar to_area_id FK "도착 구역 ID(AREA)"
		varchar status "작업 상태"
		datetime pick_time "로딩 시각"
		datetime drop_time "언로딩 시각"
	}

	AMR_STATUS_LOG {
		bigint amr_statlog_id PK "로그 ID"
		int amr_id FK "AMR ID"
		varchar area_id FK "현재 구역 ID(AREA)"
		varchar status "OPERATING/IDLE/CHARGING/ERROR/EMERGENCY_STOP"
		varchar fault_code "고장 코드(ERROR 시)"
		varchar fault_message "고장 설명(선택)"
		datetime fault_recovered_at "고장 복구 시각(NULL=미복구)"
		datetime emergency_resolved_at "비상 정지 조치 완료 시각(NULL=조치 필요)"
		int pos_x "x좌표"
		int pos_y "y좌표"
		int yaw "방향(degree)"
		int load_weight "적재 중량"
		int battery_pct "배터리 잔량"
		int SOH_pct "배터리 건강도"
		float battery_temp "배터리 온도"
		datetime updated_at "업데이트 시각"
	}

	USER_ACCOUNT {
		varchar user_id PK "사용자 ID"
		varchar username "사용자명"
		varchar password_hash "비밀번호 해시"
		varchar display_name "표시 이름"
		varchar role "역할"
		date created_at "생성일"
	}

	REFRESH_TOKEN {
		varchar token PK "토큰"
		varchar user_id FK "사용자 ID"
		datetime expires_at "만료 시각"
		boolean revoked "무효화 여부"
		datetime created_at "생성 시각"
	}

	ALARM_LOG {
		bigint alarm_id PK "알람 ID"
		varchar source_type "소스 유형"
		varchar source_id "소스 ID"
		varchar level "알람 레벨"
		varchar message "알람 메시지"
		datetime occurred_at "발생 시각"
		boolean acknowledged "확인 여부"
		datetime acknowledged_at "확인 시각"
	}

	AMR_COMMAND {
		varchar command_id PK "명령 ID"
		int amr_id FK "AMR ID"
		varchar command_type "명령 유형"
		text params "명령 파라미터"
		boolean accepted "DB 반영 완료 여부(API accepted와 동일)"
		varchar status "명령 처리 상태(예: PENDING)"
		datetime requested_at "요청 시각"
		datetime executed_at "실행 시각"
	}

	SITE||--o{AREA:"contains"
	AREA||--o{PR_PROCESS:"contains"
	AREA||--o{AMR_CHARGE_STATION:"has"
	AREA||--o{ENV_SENSOR:"has"
	ENV_SENSOR||--o{ENV_SENSOR_LOG:"measures"
	PRODUCT||--o{WORK_ORDER:"has"
	PRODUCT||--o{PR_ROUTING:"follows"
	PR_ROUTING||--o{WIP_LOT:""
	PR_PROCESS||--o{PR_ROUTING:"defined_in"
	WORK_ORDER||--o{WIP_LOT:"creates"
	WIP_LOT||--o{AMR_TASK:"executes"
	AMR_MASTER||--o{AMR_STATUS_LOG:"creates"
	AMR_MASTER||--o{AMR_TASK:"executes"
	AMR_MASTER||--o{AMR_CHARGING_LOG:"creates"
	AMR_MASTER||--o{AMR_COMMAND:"receives"
	AMR_CHARGE_STATION||--o{AMR_CHARGING_LOG:"creates"
	USER_ACCOUNT||--o{REFRESH_TOKEN:"issues"
```

## AMR 제어 명령 및 운행 상태

시뮬레이션 환경에서 **운행 상태의 기준 저장소는 DB**이다. DAS(Node-RED)의 좌표·작업 시뮬 중단은 FE가 MQTT로 수행하며, BE는 DAS에 접속하지 않는다. 전체 흐름은 `docs/ADR/20260518-1252-AMR-emergency-logic.md`, API는 `docs/API 정의.md` §3·§8을 따른다.

### AMR_COMMAND

| 항목 | 설명 |
| --- | --- |
| 목적 | `POST /api/v1/amrs/{amrId}/commands`로 수신한 제어 명령의 **이력·감사** |
| 작성 주체 | BE (트랜잭션 내 INSERT) |
| 주요 컬럼 | `command_id`, `amr_id`, `command_type` (`emergencyStop` 등), `params`, `accepted`, `status`, `requested_at`, `executed_at` |
| `accepted` | HTTP 응답 `accepted: true`와 동일하게, **해당 트랜잭션에서 명령·운행 상태 DB 반영이 완료**되었음을 의미한다. DAS 시뮬 중단 완료를 뜻하지 않는다. |
| `status` | 명령 레코드의 처리 단계(예: `PENDING`). DAS MQTT 전달 결과와는 별도로 관리할 수 있다. |

비상 정지(`emergencyStop`) 시 BE는 commit 전에 `AMR_COMMAND`에 행을 넣고, `AMR_STATUS_LOG`(및 정책에 따른 `AMR_TASK`)를 같은 트랜잭션으로 갱신한 뒤 `accepted = TRUE`로 저장한다.

### AMR_STATUS_LOG

| 항목 | 설명 |
| --- | --- |
| 목적 | 관제·REST·WebSocket이 참조하는 **AMR별 최신 운행 스냅샷** (위치, 구역, 상태, 배터리 등) |
| 작성 주체 | BE (명령 처리·주기 동기화·시드 등), DAS는 BE가 직접 쓰지 않음 |
| 갱신 방식 | `emergencyStop` 등 명령 처리 시 **기존 행 UPDATE** 또는 **신규 INSERT** (구현·데이터 정책에 따름) |
| `status` | `OPERATING`, `IDLE`, `CHARGING`, `ERROR`, `EMERGENCY_STOP` (상호 배타). `emergencyStop` commit 시 `EMERGENCY_STOP`. AMR 자체 고장 시 `ERROR` |
| `fault_code` | `status = ERROR`일 때만. `SENSOR_FAULT`, `COMM_LOST`, `COLLISION`, `OVERLOAD`, `LOAD_IMBALANCE`, `DRIVE_FAULT`, `NAVIGATION_FAULT` (`docs/API 정의.md` §3) |
| `fault_recovered_at` | 고장 복구·정비 완료 시각. NULL이면 **미복구 ERROR** |
| `emergency_resolved_at` | 비상 정지 후 운영자 조치 완료 시각. NULL이면 **비상 정지 조치 필요** |
| DAS와의 관계 | DAS가 MQTT로 받은 정지 이후 좌표가 멈추더라도, **관제 기준 상태**는 이 테이블(및 API)을 따른다. FE는 `accepted: true` 수신 후에만 DAS에 MQTT를 보낸다. |

### 대시보드 AMR 상태 집계

`GET /dashboard/summary`의 `amrError`, `amrErrorUnresolved`는 별도 저장 컬럼이 아니라, AMR별 **최신** `AMR_STATUS_LOG` 행에서 조회 시 집계한다.

| API 필드 | 집계 |
| --- | --- |
| `amrOperating` | `status = 'OPERATING'` |
| `amrWaiting` | `status = 'IDLE'` |
| `amrCharging` | `status = 'CHARGING'` |
| `amrError` | `status IN ('ERROR', 'EMERGENCY_STOP')` |
| `amrErrorUnresolved` | (`status = 'ERROR'` AND `fault_recovered_at IS NULL`) OR (`status = 'EMERGENCY_STOP'` AND `emergency_resolved_at IS NULL`) |

### AMR_TASK (비상 정지 시)

진행 중 작업이 있을 경우, 팀 정책에 따라 `AMR_TASK.status`를 취소·중단 등으로 갱신할 수 있다. BE는 `AMR_COMMAND`·`AMR_STATUS_LOG`와 **동일 트랜잭션**에서 처리하는 것을 권장한다.

### 환경 데이터 API 매핑

| API | 데이터 출처 |
| --- | --- |
| `GET /environment/areas/current` | `AREA`, `ENV_SENSOR`, `ENV_SENSOR_LOG`(센서별 최신 `measured_at`) |

### 분석 KPI 집계 (`GET /analytics/kpis`)

| API 필드 | 집계 |
| --- | --- |
| `errorCount` | 버킷 내 `AMR_STATUS_LOG` where `status = 'ERROR'` |
| `scheduleComplianceRate` | 버킷 내 완료 `AMR_TASK` 대비 `PR_ROUTING.standard_lead_time` 이내 완료 비율(%) |

| API | 데이터 출처 |
| --- | --- |
| `GET /analytics/workload` | `AMR_TASK` (`pick_time` 기준, `groupBy=hour`·`amr`) |

### 물리 스키마

컬럼 타입·FK·시드 예시는 `DB/init.sql`의 `AMR_STATUS_LOG`, `AMR_COMMAND` 정의를 따른다.
