# 설계 및 산출물 보완 체크리스트

개발 착수 전, 이전 점검에서 도출된 보완 항목을 순차적으로 처리할 때 사용합니다. 항목을 완료하면 `[ ]`를 `[x]`로 바꿉니다.

---

## 1. 루트 README

- [ ] `README.md`에 프로젝트 한 줄 소개와 목적을 적는다.
- [ ] `docs/` 문서 목록과 권장 읽기 순서를 적는다.
- [ ] GitHub 저장소 문서와 Jira, Confluence 등 외부 도구의 역할 분담을 적는다. (어느 쪽이 최신 원본인지 포함)
- [ ] 개발 환경이 갖춰진 뒤에는 실행 방법, 필수 도구, 환경 변수 개요를 추가한다. (해당 시점에 체크)

---

## 2. 영역별 작업 계획 (TODO.md)

`AGENTS.md`에서 각 영역 폴더의 `TODO.md`를 최우선으로 보도록 되어 있다.

각 항목은 해당하는 작업자가 직접 작성하는 것으로 한다.

- [ ] `FE/TODO.md`를 작성한다. 설계 문서 기준 남은 프론트엔드 작업을 세분화한다.
- [ ] `BE/TODO.md`를 작성한다. 설계 문서 기준 남은 백엔드 작업을 세분화한다.
- [ ] `DB/TODO.md`를 작성한다. 스키마, DDL, Node-RED, 시뮬레이션 데이터 범위를 세분화한다.

---

## 3. 아키텍처 결정 기록 (ADR)

`docs/협업 컨벤션.md`의 ADR 규칙과 저장소 실물을 맞춘다.

- [ ] ADR 보관 위치를 정한다. (예: `docs/adr/`)
- [ ] 파일명 규칙을 컨벤션과 동일하게 적어 둔다. (`YYYYMMDD-HHMM-짧은-제목.md`)
- [ ] 이미 확정된 결정을 최소 1건 이상 ADR로 남긴다. (예: Spring Boot, Vue, JWT, merge commit 방식 등 팀에 중요한 것부터)
- [ ] 이후 스택, 경계, 규칙이 바뀔 때 ADR을 추가하는 절차를 README 또는 협업 컨벤션과 중복 없이 안내한다.

---

## 4. 데이터 스키마 설계 문서 보강

`docs/데이터 스키마 설계.md`가 ERD 중심이면, `DB/AGENTS.md`의 DDL 규칙과 구현 가능한 수준으로 맞춘다.

- [ ] 테이블별로 컬럼명, 타입, NULL 허용, 기본값, 설명을 표 또는 목록으로 정리한다.
- [ ] PK, FK, UNIQUE 제약을 문서에 명시한다.
- [ ] 자주 조회되는 조건(시각, 상태, FK)에 대한 인덱스 설계를 적는다.
- [ ] 필요 시 동일 내용을 바탕으로 한 DDL 초안 파일 위치를 정한다. (예: `DB/sql/` 도입 여부)
- [ ] ERD와 본문 표의 타입, 관계가 일치하는지 검토한다. (예: `ROUTING.product_id`와 `PRODUCT` 키 타입 등)

---

## 5. API와 데이터 모델 정합성

`docs/API 정의.md`의 리소스가 `docs/데이터 스키마 설계.md`의 엔티티와 모두 연결되도록 한다.

- [ ] 인증(JWT, refresh)에 필요한 사용자, 토큰, 역할 저장 구조를 스키마에 반영하거나, 비저장 정책이면 문서에 명시한다.
- [ ] 알람 API에 대응하는 알람 테이블(또는 외부 연동)을 스키마에 반영한다.
- [ ] 작업 이력, 분석 KPI 등 API 응답 필드의 출처 테이블 또는 집계 뷰를 매핑 표로 정리한다.
- [ ] WebSocket 이벤트별로 어떤 데이터가 갱신되는지, 어떤 테이블 또는 스냅샷과 대응하는지 한 페이지에 요약한다.

---

## 6. 화면 설계와 API, 실시간 채널 연결

`docs/프로젝트 정의서.md`의 화면 설계와 구현 경로를 연결한다.

- [ ] 화면별 라우트(URL) 초안을 정한다.
- [ ] 각 화면(또는 주요 구역)이 호출하는 REST 엔드포인트 목록을 표로 만든다.
- [ ] 실시간 영역에 WebSocket 구독이 필요한지, 어떤 이벤트 타입을 쓰는지 화면별로 적는다.
- [ ] 대시보드 평면도 등 시각 자산이 없다면, 와이어프레임 또는 참고 이미지 링크를 이슈 또는 문서에 연결한다.

---

## 7. Node-RED 및 DAS 경계

- [ ] Node-RED가 쓰는 플로우 파일 위치와 네이밍 규칙을 정한다.
- [ ] 시뮬레이션 데이터의 삽입 주기, 대상 테이블, 값 범위를 문서화한다.
- [ ] BE가 읽는 데이터 소스가 DB 단일인지, 향후 메시지 큐 등이 있는지 경계를 `docs/`에 한 절로 고정한다.

---

## 8. 기타 정리

- [ ] `.github` 이슈 템플릿의 작업 범위 라벨(`backend`, `frontend` 등)과 저장소 폴더명(`BE`, `FE`)이 혼용되므로, 팀에서 쓰는 표기를 README 또는 협업 컨벤션에 한 줄로 통일한다.
- [ ] 위 항목을 반영한 뒤 `docs/프로젝트 정의서.md`, `docs/API 정의.md`, `docs/데이터 스키마 설계.md` 상호 링크나 문서 버전, 최종 수정일을 필요 시 갱신한다.

---

## 참고

- 상세 협업 규칙: `docs/협업 컨벤션.md`
- 설계 원본: `docs/프로젝트 정의서.md`, `docs/API 정의.md`, `docs/데이터 스키마 설계.md`
